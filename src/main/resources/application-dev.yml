server:
  port: 8080

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1：3306/zhenrongbao?useUnicode=true&characterEncoding=utf8
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/UserMapper.xml
  type-aliases-package: com.lal.entity

#showSql
logging:
  level:
    com:
      example:
        mapper : debug